# Agents 说明

## 项目规则

- 修改代码、配置、文档、自动化脚本或部署行为之前，先查看现有项目结构，并尽量沿用已有写法。
- 搜索文件和内容时，优先使用 `rg` 和 `rg --files`。
- 改动范围要尽量贴近当前任务，不要顺手重写无关文件或运行时输出。
- 除非用户明确要求，不要修改 `logs/`、`log_mirror/` 这类生成日志或镜像运行数据。
- 保留当前项目主要入口，除非任务明确要求调整：
  - `run.py`
  - `server.py`
  - `scanner.py`
  - `config.json`

## UPDATE_LOG.md 记录要求

- 项目根目录必须维护 `UPDATE_LOG.md`。
- 以后每次修改代码、配置、文档、自动化脚本或项目行为，都要在同一次工作里同步更新 `UPDATE_LOG.md`。
- 更新日志统一使用中文。
- 日期写在 `## YYYY-MM-DD` 标题中；同一天内有多条更新时，必须在日期标题下用 `### HH:MM - 更新标题` 分隔。
- 条目正文不再重复日期和时间；旧记录如果确实不知道时间，可以使用 `### 时间未记录 - 更新标题`。
- 每条记录尽量包含：
  - 改动摘要
  - 涉及的文件或目录
  - 对用户可见的行为变化，如果有的话
  - 做过的验证，如果有的话
- 不要只依赖 git 历史记录改动。`UPDATE_LOG.md` 是给人直接看的项目更新记录。
- 新记录放在对应日期下的最上面；如果当天已有日期标题，不要重复新增同一个日期标题。内容保持简洁。

## 日志监测与项目维护

- Watch 及当前纳入监测的 GRACED、Cities、China、Power 项目均已迁移到 `gpu104`；日常同步、检查和发布只使用 `gpu104` 本地路径，不依赖旧服务器。
- 当用户要求“看一下日志”“检查最近运行情况”“监测项目”或类似任务时，默认检查 `gpu104` 上当前已经发给 Codex 的项目日志。只有用户明确要求核对历史服务器时，才访问其他服务器。
- 优先查看最近几天的项目日志，重点搜索并判断 `bug`、`error`、`warning`、`exception`、`traceback`、`failed`、异常退出、长时间卡住、数据缺失等异常信号。
- 如果日志中出现异常，先定位对应项目路径，再进入该项目读取项目级 `agents.md` / `AGENTS.md` 和 `UPDATE_LOG.md` / `update_log.md`，确认本项目的维护规则、历史改动和记录格式。
- 读完项目规则和更新日志后，再检查相关代码、配置、数据、运行脚本和上下游日志，尽量给出可执行的修复方案、风险判断或维护建议。
- 如果问题可以安全修复并符合用户当前要求，按项目既有写法做小范围改动，并运行最小但有效的验证。
- 只要相关项目发生代码、配置、文档、自动化脚本、数据处理逻辑或部署行为更新，就必须同步更新该项目自己的 `UPDATE_LOG.md` / `update_log.md`；如果只是阅读和分析，没有实际改动，则在最终回复中说明未更新日志的原因。
- 不要直接改写原始运行日志、日志镜像或生成数据，除非用户明确要求或项目级规则允许。

## 验证要求

- 根据改动范围，运行最小但有效的验证。
- 如果某项验证不适合运行或没有运行，需要在最终回复里说明；必要时也写进 `UPDATE_LOG.md`。
